# Changelog

## v1.13.10 - 2026-09-19

- 修复带默认值的配置项被类属性挡住：`feedback_session_id`、图片分析开关、缓存数量等 WebUI 改完不生效。
- 回执会话 ID 说明改为使用平台实例 ID，避免照抄 `aiocqhttp`。

## v1.13.9 - 2026-09-19

- 开启「查看说说时分析图片」后，优先把说说图片注入当前对话上下文，让模型直接看图。
- 无法注入时才回退到原来的视觉模型转写。

## v1.13.8 - 2026-09-19

- 移除全部用户指令（看说说、发说说、删说说、评说说、回评、赞说说、重置 QQ Cookies、空间会话ID）。
- 空间操作只通过 AI 工具完成；回执仍由 `feedback_session_id` 控制，未配置则静默。
- 删除仅服务于指令的配置项 `send_feedback` 和命令参数解析逻辑。

## v1.13.7 - 2026-09-19

- AI 调用空间工具时，回执改发到配置的「回执会话 ID」，不再出现在当前聊天。
- 未配置回执会话时静默执行，工具结果只返回给模型，由 AI 用自然语言回复。
- 新增管理员命令 `空间会话ID` / `空间会话ID 设置`，方便复制或保存当前会话。
- 补充工具说明，引导模型在用户用自然语言提到空间/说说时主动调用。
- 修正删除工具注册名 `llm_delete_feef` 为 `llm_delete_feed`。

## v1.13.6 - 2026-09-18

- 发说说 / `llm_publish_feed` 支持本地图片路径：可传 Windows、Linux、`file://`，以及 `\AstrBot\data\...` 这类容器内路径。
- LLM 工具新增 `image_paths` 参数，不再只能从当前消息或引用消息里取图。
- 图片源优先使用已存在的本地文件，避免把本机路径当成远程 URL 下载。
